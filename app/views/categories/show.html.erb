<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @category.name %>
</p>

<h2>Products</h2>

<%= render :partial => "products/product_table", :locals => {:products => @category.products} %>

<h2>Add a product:</h2>
<%= render "products/form" %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
